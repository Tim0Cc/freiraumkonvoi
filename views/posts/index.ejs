<h2 class="page-header">Alle Protokolle</h2>
<div class="form-row btn-row btn-row-end">

    <a class="btn btn-primary" href="/posts/new">neues Protokoll erstellen</a>

</div>
<div class="page-container">
  <div class="posts-container">
    <div class="post-container post-header">
      <div class="post-title">neueste Protokolle</div>
      <div class="post-ag">AG</div>
      <div class="post-container-info">Info</div>
    </div>
    <% posts.forEach(post => { %>
      <div class="post-container">
        <div class="post-title">    
          <a class="btn btn-primary" href="/posts/<%= post.id %>"><%= post.title %></a>
        </div>
        <% users.forEach(user => { %> 
          <% if(user.id == post.user) { %>
            <div class="post-ag">
              <%= user.name %>-AG  
            </div>
          <% } %> 
        <% }) %>
        <div class="post-container-info">
          <div class="post-content">
            <% let postUpdated = post.updatedAt %> 
            zul. geändert: <%= `${postUpdated.getDate()}.${postUpdated.getMonth()+1}.${postUpdated.getFullYear()}` %>
          </div>
          <div class="post-content">
            <% let postCreated = post.createdAt %> 
            erstellt am: <%= `${postCreated.getDate()}.${postCreated.getMonth()+1}.${postCreated.getFullYear()}` %>
          </div>
        </div> 
      </div>
    <% }) %> 
  </div>
  <div class="form-row btn-row btn-row-end">
    <a class="btn btn-primary" href="/posts/new">neues Protokoll erstellen</a>
  </div>
</div>