<h3>Kommentare:</h3>
<div>
  <% comments.forEach(comment => { %>
    <% if(post.id == comment.post) { %>
      <div>
        <%= comment.commentText %> 
      </div>
      <div>
        <% users.forEach(user => { %> 
          <% if(user.id == comment.user) { %>
            <%= user.name %>-AG  
          <% } %> 
        <% }) %> 
      </div>
    <% } %> 
    <br>
  <% }) %> 
</div>
<h4>Neuen Kommentar hinzufügen</h4>
<div>
  <form action="/comments" method="POST">
    <div class="form-row">
      <label for="commentText"></label>
      <textarea name="commentText"></textarea>
      <input type="hidden" name="post" value="<%= post.id %>">
      <input type="hidden" name="targetUser" value="<%= post.user.id %>">
    </div>
    <div class="form-row form-row-end btn-row">
      <button class="btn btn-primary" type="submit">kommentieren</button>
    </div>
  </form>
    <!-- <a class="btn btn-primary" href="/comments/new">Kommentar verfassen</a> -->
</div>